# Build output
/kvbench
/kvbench.exe
/crashchild
/crashchild.exe

# Go test/coverage artefacts
*.out
*.test
*.test.exe

# Scratch stores written by hand while poking at things
/scratch/

# Crash directories preserved by a failing corpus seed. CI uploads them;
# locally they are wreckage worth keeping until the bug is found.
/crash-failures/
/crashtest/crash-failures/
